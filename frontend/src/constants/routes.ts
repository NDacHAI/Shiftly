export const routes = {
    dashboard: '/',
    branches: '/branches',
    positions: '/positions',
    employees: '/employees',
    employeeDetail: '/employees/:id',
    settings: '/settings',
    workShifts: '/work-shift',
} as const;
